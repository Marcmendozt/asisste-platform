<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms"
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:d="http://xamarin.com/schemas/2014/forms/design"
       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
       mc:Ignorable="d"
       xmlns:local="clr-namespace:XAMAsisste.Vistas"
             x:Class="XAMAsisste.Cascara">

    <Shell.Resources>
        <ResourceDictionary>
            <Color x:Key="NavigationPrimary">#000000</Color>
            <Style x:Key="BaseStyle"
                   TargetType="Element">
                <Setter Property="Shell.BackgroundColor"
                        Value="{StaticResource NavigationPrimary}" />
                <Setter Property="Shell.ForegroundColor"
                        Value="Chocolate" />
                <Setter Property="Shell.TitleColor"
                        Value="White" />
                <Setter Property="Shell.DisabledColor"
                        Value="#B4FFFFFF" />
                <Setter Property="Shell.UnselectedColor"
                        Value="#95FFFFFF" />
                <Setter Property="Shell.TabBarBackgroundColor"
                        Value="{StaticResource NavigationPrimary}" />
                <Setter Property="Shell.TabBarForegroundColor"
                        Value="White" />
                <Setter Property="Shell.TabBarUnselectedColor"
                        Value="#95FFFFFF" />
                <Setter Property="Shell.TabBarTitleColor"
                        Value="White" />
            </Style>
            <Style TargetType="TabBar"
                   BasedOn="{StaticResource BaseStyle}" />
        </ResourceDictionary>
    </Shell.Resources>

    <!-- Loading/Start Page -->
    <ShellItem Route="Cargando">
        <ShellContent ContentTemplate="{DataTemplate local:Cargando}" />
    </ShellItem>

   
    <!-- Login and Registration Page -->
    <ShellContent Route="Login"
                  ContentTemplate="{DataTemplate local:Login}">
    </ShellContent>

 

    <!-- Main Page -->
    <FlyoutItem Route="Principal" 
                FlyoutDisplayOptions="AsMultipleItems" IsTabStop="False">
        <ShellContent  Route="Inicio" Icon="home.png"
                      IsTabStop="False"
                      ContentTemplate="{DataTemplate local:Principal}"
                      Title="Home" />
    </FlyoutItem>
    <FlyoutItem   Icon="smartphone.png" Title="Marcar asistencia" >
        <ShellContent   Route="marcar"
                      IsTabStop="False"
                      ContentTemplate="{DataTemplate local:Marcar}"/>
    </FlyoutItem>
 
    <FlyoutItem  Title="Faltas" Icon="notes.png"  >
        <ShellContent Route="faltas" 
                      IsTabStop="False"
                      ContentTemplate="{DataTemplate local:Faltas}"/>
    </FlyoutItem>
    <MenuItem Text="Cerrar Sesion" IconImageSource="lock.png"
              Clicked="CerrarSesion"  />
</Shell>